/* PARA LER E GUARDAR OS DADOS DA IMAGEM PPM */

#include "mod.h"

void lerPPM(FILE *imagem){
	int i,j;
	char codigo[3];
	int linha, coluna, tamanhoImg;
	PPMImage ppm;
	fscanf(imagem,"%s",codigo);
	fscanf(imagem,"%d",)
	for(i = 0; i < )
}
